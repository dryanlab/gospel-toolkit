import SwiftUI

extension Color {
    static let primaryIndigo = Color(red: 0.18, green: 0.22, blue: 0.45)
    static let accentGold = Color(red: 0.82, green: 0.68, blue: 0.21)
    static let warmWhite = Color(red: 0.98, green: 0.96, blue: 0.93)
    static let warmGray = Color(red: 0.55, green: 0.53, blue: 0.50)
}

extension Font {
    static let sectionTitle = Font.title2.weight(.bold)
    static let cardTitle = Font.headline
    static let cardBody = Font.subheadline
}
